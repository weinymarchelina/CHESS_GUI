#ifndef BOARD_H
#define BOARD_H

#include "piece.h"
#include "position.h"

/*
class Board
{
public:
    Piece* board[8][8];
    Piece* lastMovedPiece;
    Board();
    Piece* getPieceAt(Position);
    void setPieceAt(const Position&, Piece*);
    void capturePiece(Piece*, vector<Piece>&);
    void doPromotion(Piece*, const char, Position, vector<Piece>&);
};
*/

#endif // BOARD_H
